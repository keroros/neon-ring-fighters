# 霓虹擂台 Agent 配置

## 项目目标
- 构建一个原创复古街机格斗网页原型，项目名称为“霓虹擂台”。
- 优先保证本地双人对战闭环完整，手感清晰，打开网页即可试玩。
- 不复制拳皇角色、名称、美术、音频、标志或其他受版权保护的素材。

## 技术方向
- 使用原生 `HTML`、`CSS` 和 `JavaScript Canvas`。
- 保持项目零依赖，`index.html` 可以直接用浏览器打开。
- 角色、舞台、打击特效和必杀波动都使用原创 Canvas 绘制。

## 文件说明
- `index.html`：承载游戏画布、状态栏、操作按钮和按键说明。
- `styles.css`：负责复古街机视觉、响应式布局、血条、能量条和控制区样式。
- `game.js`：负责游戏循环、输入、角色状态、物理、碰撞、攻击、格挡、必杀、倒计时和胜负结算。

## 玩法范围
- 1P 操作：`A/D` 移动，`W` 跳跃，`S` 蹲下，`E` 格挡，`F` 轻拳，`G` 重拳，`H` 必杀。
- 2P 操作：方向键移动、跳跃和蹲下，`I` 格挡，`J` 轻拳，`K` 重拳，`L` 必杀。
- 对局规则：每局 60 秒，双方各 100 点生命；KO 立即结束，时间到按剩余生命判胜。
- 战斗规则：轻拳速度快但伤害低，重拳速度慢但伤害高，必杀消耗能量并发射短程波动。
- 格挡规则：必须面向对手且未攻击时才生效；成功格挡会减伤并产生后退。

## 测试要求
- 打开 `index.html` 后必须能直接试玩，无需安装依赖。
- 验证移动边界、跳跃、落地、蹲下、格挡、攻击、必杀、命中特效、暂停、重开、KO 和时间到结算。
- 检查桌面和窄屏布局，确保状态栏、画布、按钮和按键说明不重叠。

## 默认约定
- 当前文件是后续 agent 工作的项目级配置。
- 首版不包含联网、人机 AI、角色选择、复杂连招表、外部素材和音频。
- 项目文件夹名称使用英文名：`neon-ring-fighters`。
